apiVersion: v1
kind: PersistentVolume
metadata:
  name: jenkins-volume
  labels:
    app: jenkins-pod
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/home/jenkins-app/volume"
